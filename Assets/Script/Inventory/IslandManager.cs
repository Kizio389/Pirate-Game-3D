using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IslandManager : MonoBehaviour
{
    /*[SerializeField] GameObject isLand_In;
    [SerializeField] GameObject isLand_Out;
    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.CompareTag("Player"))
        {
            if(isLand_In.activeSelf == false)
            {
                isLand_In.SetActive(true);
            }
            isLand_Out.SetActive(false);
            GameObject gameObject;
            gameObject = isLand_In;
            isLand_In = isLand_Out;
            isLand_Out = gameObject;
        }
    }*/
}
